@model IEnumerable<SqlScripts>

@{
    ViewBag.Title = "SqlScripts Table";
}

<table>
    <tr>
        <th>Id</th>
    </tr>

    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
            </tr>
        }
    </tbody>
</table>
